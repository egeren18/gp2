<?php

class Main {

    public static function jAboutUs() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jAboutUs.php';
    }

    public static function jChangePassword() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jChangePassword.php';
    }

    public static function jChangePasswordDb() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jChangePasswordDb.php';
    }

    public static function jCheckSession() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jCheckSession.php';
    }

    public static function jCookies() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jCookies.php';
    }

    public static function jDataForwarding() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jDataForwarding.php';
    }

    public static function jDataForwardingDb() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jDataForwardingDb.php';
    }

    public static function jError() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jError.php';
    }

    public static function jForgotPassword() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jForgotPassword.php';
    }

    public static function jFooter() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jFooter.php';
    }

    public static function jFooterInfo() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jFooterInfo.php';
    }

    public static function jHeader() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jHeader.php';
    }

    public static function jLogInEmail() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jLogInEmail.php';
    }

    public static function jLogInPassword() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jLogInPassword.php';
    }

    public static function jLogInProblems() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jLogInProblems.php';
    }

    public static function jLogOut() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jLogOut.php';
    }

    public static function jMenu() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jMenu.php';
    }

    public static function jMenuGp() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jMenuGp.php';
    }

    public static function jNavBar() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jNavBar.php';
    }

    public static function jNotAllowed($module) {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jNotAllowed.php';
    }

    public static function jPrivacy() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jPrivacy.php';
    }

    public static function jResponsibility() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jResponsibility.php';
    }

    public static function jSecurityQuestion() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jSecurityQuestion.php';
    }

    public static function jSessionStart() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jSessionStart.php';
    }

    public static function jTimer() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jTimer.php';
    }

    public static function jValidateAnswer() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jValidateAnswer.php';
    }

    public static function jValidateEmail() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jValidateEmail.php';
    }

    public static function jQuestion() {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/main/jQuestion.php';
    }

}

$main = new Main();
