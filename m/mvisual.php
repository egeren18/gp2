<?php

class Mvisual {

    public static function aIndex($moduleId) {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/mvisual/aIndex.php';
    }

    public static function aIndex2($moduleId) {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/mvisual/aIndex2.php';
    }

    
    public static function aCreateDb($moduleId) {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/mvisual/aCreateDb.php';
    }

    public static function aCreateDb2($moduleId) {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/mvisual/aCreateDb2.php';
    }

    public static function aUpdateDb($moduleId) {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/mvisual/aUpdateDb.php';
    }

    public static function aUpdateDb2($moduleId) {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/mvisual/aUpdateDb2.php';
    }

    public static function aDeleteDb($moduleId) {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/mvisual/aDeleteDb.php';
    }

    public static function aDeleteDb2($moduleId) {
        
        /*
         * Verificará si el archivo ya ha sido incluido y si es así, no se incluye (require) de nuevo.
         */
        require_once '../f/mvisual/aDeleteDb2.php';
    }


}

$mvisual = new Mvisual();